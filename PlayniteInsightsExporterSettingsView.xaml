<UserControl x:Class="PlayniteInsightsExporter.PlayniteInsightsExporterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="Playnite Insights Server URL" Margin="12,12,12,6"/>
        <TextBox Text="{Binding Settings.WebAppURL}" Margin="12,0,12,6"/>
        <Button 
            Content="Send JSON to Server" 
            Width="150" 
            Command="{Binding Settings.ExportLibraryButton}"
            Margin="12,12,12,12"
            HorizontalAlignment="Left"/>
    </StackPanel>
</UserControl>